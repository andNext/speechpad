<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/text_orange</item>
        <item name="android:background">@color/background_black</item>
        <item name="android:textSize">@dimen/text_size_out</item>
        <item name="android:editTextColor">@color/text_orange</item>
    </style>

</resources>
